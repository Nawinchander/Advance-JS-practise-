 // nee la uyiorda irunthu ena pana pora
        // alert("hi");
        let score = 0 // initialization
        // assignment
        // score=10
        // console.log("score is = "+score);
        // let amount = 1000;
        // console.log("the amount is "+ amount);

       //  const pi = 3.14;

        // prompt - user input'
        // let name = prompt("what your name ?");
        // console.log('Hello '+ name);

        // let tickets = prompt('How many tickets do you want')
        // console.log ('you should pay', tickets*100);


       //  let a = prompt('enter a number')
       //  console.log(Number(a)+10);


       let str1 = "Strive not to be a success"
       let str2 = "but rather to be a value"


       // partitioning String

       // slice(start, end)
       // substring(start, end)
       // substr(start, end)
       // replace('strive',"aim")
       // str1.toUpperCase()
       // str1.length
       // str1.toLowerCase()
       // str1.trim()
       // str1.trimStart()
       
       // template literals
       // introduction in 2015 with ECMAScrit or ES6

       let firstname = 'ramya'
       let lastname = 'Murali'
       let city = 'chennai'
       console.log( firstname + "" + lastname + "lives in"+ city )

       // backtick
       let msg = `${firstname} ${lastname} lives in ${city}`
       console.log(msg)

       // array
       let num = 10
       let name = 'vidhya'

       let cities = ["Chennai", "Madurai","Trichy"];

       let marks = [78,56,67,54,98];

       console.log(cities.length)

       // access 2nd element from start - index start with 0
       console.log(cities[1])

       // access 3rd element in the array
       console.log(cities[3])

       // last element
       console.log(marks[marks.length-1])

       // mix of int and string
       let arr = [5,6,7,'a','abc',[3,4]]
       console.log(arr)
       console.log(arr[5][1])

       // 2nd array
       let matrix = [[3, 4, 5], [4, 3, 2], [1, 2, 3]]
       console.log(matrix)
       console.log(matrix[0][2])
       console.log(matrix[1][2])

       // array method
       let array = ['a','b','c','d','e']

       // push and add element at the end
       array.push('f')
       console.log(array)

       // pop - remove elements form the
       console.log(array.pop())

       // shift remove the element from the start of the array
       // return remove value
       console.log(array.shift())
       console.log(array)

       // unshift adds element to the start of the array
       // return new length
       console.log(array.unshift('a'))

       // delete
       // delete array[2]
       // console.log(array)

       // splice
       array.splice(2)
       console.log(array)

       array.splice(1,1,'m')
       console.log(array)

       // slice
       console.log(array.slice(1,2))

       // reverse
       array.reverse()
       console.log(array)

       // join convert array to string

       let str = array.join()
       console.log(str)

       // split
       let str3 = "'g','t','r','e'"
       str3.split(',')
       console.log(str3)

       // concat and spread operator
       let firstarr = [1,2,3];
       let secondarr = [4,5,6];

       let joinedarr = [firstarr, secondarr]
       console.log(joinedarr)

       joinarr = firstarr.concat(secondarr)
       console.log(joinarr)

       





















































        


